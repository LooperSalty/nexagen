/** Maximum number of players per game room */
export const MAX_PLAYERS_PER_ROOM = 8;

/** Server tick rate in Hz */
export const TICK_RATE = 20;

/** Physics simulation timestep in seconds */
export const PHYSICS_STEP = 1 / 60;

/** Maximum distance (blocks) a player can interact with objects */
export const INTERACTION_DISTANCE = 5;

/** Gravity acceleration in blocks/s^2 */
export const GRAVITY = 20;

/** Base player movement speed in blocks/s */
export const PLAYER_SPEED = 5;

/** Speed multiplier while sprinting */
export const SPRINT_MULTIPLIER = 1.6;

/** Jump force (initial upward velocity) in blocks/s */
export const JUMP_FORCE = 8;

/** Duration of a full day/night cycle in seconds */
export const DAY_DURATION = 600;

/** Total inventory slots (including hotbar) */
export const MAX_INVENTORY_SLOTS = 36;

/** Number of hotbar slots */
export const HOTBAR_SIZE = 9;

/** Maximum chat message length in characters */
export const CHAT_MAX_LENGTH = 256;

/** Maximum block reach distance for placing/breaking */
export const PLAYER_REACH = 6;

/** Minimum fall distance (blocks) before damage is applied */
export const FALL_DAMAGE_THRESHOLD = 4;

/** Damage per block fallen beyond the threshold */
export const FALL_DAMAGE_MULTIPLIER = 1.5;

/** Respawn invincibility duration in seconds */
export const RESPAWN_INVINCIBILITY = 3;

/** Base XP needed for level 1 */
export const XP_PER_LEVEL_BASE = 100;

/** XP requirement multiplier per level */
export const XP_LEVEL_MULTIPLIER = 1.15;
